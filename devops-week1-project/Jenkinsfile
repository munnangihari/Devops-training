pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/munnangihari/Devops-training.git'
            }
        }

        stage('Unit Tests') {
            steps {
                // Run tests inside Python Docker container
                sh 'docker run --rm -v $PWD:/app -w /app python:3.11 bash -c "pip install -r application/requirements.txt && pytest tests/"'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'docker build -t devops-week1-app .'
            }
        }

        stage('Deploy to EC2') {
            steps {
                sh 'ssh -o StrictHostKeyChecking=no ubuntu@<EC2_PUBLIC_IP> "cd ~/devops-week1-project && git pull && docker build -t devops-week1-app . && docker run -d -p 80:8000 devops-week1-app"'
            }
        }

        stage('Health Check') {
            steps {
                sh './scripts/health-check.sh'
            }
        }
    }
}

