aws_region  = "us-east-1"
environment = "dev"
db_username = "appadmin"
# db_password: don't put this here. Set it via
#   export TF_VAR_db_password="yourpassword"
# before running plan/apply, so it never gets committed to git.
